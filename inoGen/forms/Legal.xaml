<Window x:Class="Legal"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pakete und Lizenz"
        Height="500" Width="800"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- Liste der Pakete -->
        <GroupBox Header="Verwendete NuGet Pakete" Margin="5">
            <ListBox x:Name="lstPackages" FontFamily="Consolas"/>
        </GroupBox>

        <!-- Lizenz -->
        <GroupBox Header="Lizenz" Grid.Column="1" Margin="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="txtLicense" 
                           TextWrapping="Wrap"
                           FontFamily="Consolas"/>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</Window>
